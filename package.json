{
  "name": "sightlines",
  "private": true,
  "version": "0.1.0",
  "description": "Chess x sudoku saga - vertical slice",
  "scripts": {
    "bundle": "python3 tools/bundle.py",
    "build:native-bridge": "esbuild native-bridge/src/index.js --bundle --format=iife --outfile=webapp/js/native-bridge.js",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.1.0",
    "@capacitor-firebase/analytics": "^8.4.0",
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "firebase": "^12.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
